# LCM PowerPoint A11y Checker (Port v4)

Adds:
- Severity buckets + filters (Critical/Serious/Moderate/Minor)
- CSV export (issues)
- "Skipped in this environment" callouts in the Checks list
- Per-user resolved/intentional markers via RoamingSettings (from v3)

Sideload: PowerPoint -> Home -> Add-ins -> More Add-ins -> Upload My Add-in -> select manifest.xml

- Scoring Summary block (open counts by severity + resolved total)

- Trend delta vs last scan (open counts)
- Gate score (PASS/FAIL)
- CSV includes a Scoring Summary row
